<div class="view" style="background-image: url('https://i.imgur.com/LxSmkAq.png'); background-repeat: no-repeat; background-size: cover; background-position: center center;">
    <div class="container text-center" style="height:200px">   
    </div>
</div>

  <div class="pricing-header px-3 py-3 pb-md-4 mx-auto text-center mt-2 mb-2">
    <h1 class="display-4">Les projets de votre région</h1>
    <p class="lead">Ces projets, créés par les communes, permettent de dynamiser la vie en milieu rural et d'améliorer le vivre ensemble. Vous trouverez une grande variété de projets, et certainement celui qui vous fera vous engager !</p>
  </div>

  <div class="row justify-content-center">
    <% if town_hall_signed_in? %>
      <%= button_to "Poster une offre", new_offer_path, class:"btn btn-success m-2", :method => :get %>
    <% end %>
  </div>
  
  <div class="row justify-content-center mt-5">
    <div class="col-md-7 border-top">
    </div>
  </div>

  <div class="row justify-content-center align-items-center" style="height:100px">
    <%= render 'search_offers' %>
  </div>

  
  <div class="container-fluid mt-2">
    <div class="row">
      <div class="col-sm-1">
      </div>

      <div class="col-sm-10 menu-offers px-5">
        <% if @offers.present? %>
          <!-- Card -->
          <% @offers.each_with_index do |offer, i| %>
              <%= render 'card_offer', offer: offer %> 
          <% end %>
          <!-- Card end -->
        <% else %>
          <p>Aucun projet ne contient le terme <%= params[:search] %>.</p>
        <% end %>
      </div>

      <div class="col-sm-1">
      </div>
    </div>
  </div>
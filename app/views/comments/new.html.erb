<div class="container">
    <div class="container-fluid mt-5">
        <div class="row">
      <div class="col-md-6 offset-md-3">  

                <h1>Créez un commentaire pour cet évenement: </h1>
                <p>Remplissez le formulaire</p>
                <br/>

                <%= form_for @comment  do |f| %>
                <!-- >
                <div class="form-group">
                    <%= f.label 'Saisissez votre prénom :' %>
                    <%= f.text_field :villager_first_name %>
                </div>
                <-->
                <div class="form-group">
                    <%= f.label_tag 'Renseignez votre commentaire : ' %>
                    <%= f.text_area :comment_content %>
                </div>
                
                
                <%= f.submit("Publier le commentaire", :class =>"btn btn-success") %>

                <% end %>
            </div>
        </div>
    </div>
</div>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "town_halls/shared/error_messages", resource: resource %>
  
    <div class="field">
      <%= f.label :ZipCode %><br />
      <%= f.number_field :zipcode, autofocus: true %>
    </div>
<div class="cart-event col-12 col-sm-8 col-md-6 col-lg-4 m-2">
          
    <!-- Card content -->
    <div class="card-body d-flex flex-row">
        <!-- Content -->
        <div>
        <!-- Title -->
        <%= link_to event_path(id: event.id), class:"link_to" do %>
            <h4 class="card-title font-weight-bold mb-2" style="text-transform: uppercase;"><%= event.title %></h4>
        <% end %>
        <!-- Subtitle -->
        <p class="card-text"><i class="far fa-money-bill-alt pr-2"></i> <%= event.price %> €</p>
        <p class="card-text"><i class="fas fa-map-marker-alt pr-2"></i> <%= event.village.name %> - <%= event.location %></p>
        <p class="card-text"><i class="far fa-clock pr-2"></i>
            <time datetime=<%= event.start_date %>
            <span class="day"> <%= event.start_date.day %> </span>
            <span class="month"> <%= event.start_date.strftime('%b') %> </span>
            <span class="year"> <%= event.start_date.year %> </span>
            <span class="time"> </span>
            </time>
        </p>
        </div>
    </div>
    <!-- Card image -->
    <div class="view overlay row justify-content-center">
        <% if event.event_picture.attached? %>
            <%= image_tag event.event_picture, size: "300x300", alt: "Card image cap" %>
        <% end %>
    </div>
    <!-- Card content -->
    <div class="card-body">
        <div class="collapse-content">
            <!-- Text -->
            <%= h truncate(event.description, :length => 150) { link_to 'Voir plus', event_path(event.id), class:"link_to" } %>
            <!-- Button -->
            <i class="fas fa-share-alt text-muted float-right p-1 my-1" data-toggle="tooltip" data-placement="top" title="Share this post"></i>
            <i class="fas fa-heart text-muted float-right p-1 my-1 mr-3" data-toggle="tooltip" data-placement="top" title="I like it"></i>
        </div>
    </div>
</div>
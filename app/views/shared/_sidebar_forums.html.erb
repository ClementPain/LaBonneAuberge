<div class="menu-container">
    <div class="row">
        <div class="col-sm-12 d-flex align-items-start">
            <% @village.forums.all.each do |f| %>
                <div class="row" style="height:50px">
                    <p><%= f.title %></p>
                </div>
            <% end %>
        </div>
    </div>
</div>